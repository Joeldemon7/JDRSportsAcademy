@page
@model JDRSportsAcademy.Pages.Coaches.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Coach[0].CoachName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coach[0].CoachNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coach[0].CoachEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coach[0].Sport)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Coach) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CoachName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoachNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoachEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sport.SportID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CoachID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CoachID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CoachID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
